@page "/"
@using WebChat.Client.Shared.Chat;

@inject Services.HubService hubService;

@if(!hubService.IsConnected)
{ 
    <Login></Login>
}
else
{
    <TextArea></TextArea>
}

@code {
    protected override void OnInitialized() {
        hubService.OnChange += () => StateHasChanged();
    }
}

